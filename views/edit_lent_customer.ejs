<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Lent Bill</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background: #f8f9fa;
    }
    .edit-form-container {
      max-width: 600px;
      margin: 40px auto;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(44,62,80,0.08);
      padding: 32px 28px;
    }
    .form-label {
      font-weight: 600;
    }
    .form-title {
      font-weight: 800;
      background: linear-gradient(90deg, #2c3e50, #6a89cc, #38ada9, #b53471, #f6b93b);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-fill-color: transparent;
      margin-bottom: 24px;
      text-align: center;
    }
    .btn-custom {
      background: linear-gradient(90deg, #2c3e50, #6a89cc, #38ada9, #b53471, #f6b93b);
      color: #fff;
      font-weight: 700;
      border: none;
    }
    .btn-custom:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <div class="edit-form-container">
    <h2 class="form-title">Edit Lent Bill</h2>
    <form action="//edit/<%= bill.id %>" method="POST" autocomplete="off">
      <div class="mb-3">
        <label for="customer_name" class="form-label">Customer Name</label>
        <input type="text" class="form-control" id="customer_name" name="customer_name" value="<%= bill.customer_name %>" required>
      </div>
      <div class="mb-3">
        <label for="mobile" class="form-label">Mobile</label>
        <input type="text" class="form-control" id="mobile" name="mobile" value="<%= bill.mobile %>" required>
      </div>
      <div class="mb-3">
        <label for="product" class="form-label">Product</label>
        <input type="text" class="form-control" id="product" name="product" value="<%= bill.product %>" required>
      </div>
      <div class="mb-3">
        <label for="size" class="form-label">Size</label>
        <input type="text" class="form-control" id="size" name="size" value="<%= bill.size %>">
      </div>
      <div class="mb-3">
        <label for="quantity" class="form-label">Quantity</label>
        <input type="number" step="1" min="1" class="form-control" id="quantity" name="quantity" value="<%= bill.quantity %>" required>
      </div>
      <div class="mb-3">
        <label for="price_per_unit" class="form-label">Price per Unit</label>
        <input type="number" step="0.01" min="0" class="form-control" id="price_per_unit" name="price_per_unit" value="<%= bill.price_per_unit %>" required>
      </div>
      <div class="mb-3">
        <label for="amount" class="form-label">Amount</label>
        <input type="number" step="0.01" min="0" class="form-control" id="amount" name="amount" value="<%= bill.amount %>" required>
      </div>
      <div class="mb-3">
        <label for="discount_percent" class="form-label">Discount (%)</label>
        <input type="number" step="0.01" min="0" max="100" class="form-control" id="discount_percent" name="discount_percent" value="<%= bill.discount_percent %>">
      </div>
      <div class="mb-3">
        <label for="discount_amount" class="form-label">Discount Amount</label>
        <input type="number" step="0.01" min="0" class="form-control" id="discount_amount" name="discount_amount" value="<%= bill.discount_amount %>">
      </div>
      <div class="mb-3">
        <label for="Total_after_Discount" class="form-label">Total after Discount</label>
        <input type="number" step="0.01" min="0" class="form-control" id="Total_after_Discount" name="Total_after_Discount" value="<%= bill.Total_after_Discount %>" required>
      </div>
      <div class="d-flex justify-content-between">
        <a href="/lent_customer" class="btn btn-secondary">Cancel</a>
        <button type="submit" class="btn btn-custom">Update</button>
      </div>
    </form>
  </div>
</body>
</html> -->
